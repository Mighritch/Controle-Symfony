{% extends 'base.html.twig' %}

{% block body %}
    <h1>Modifier le catégorie</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Enregistrer les modifications</button>
    {{ form_end(form) }}

    <a href="{{ path('app_categorie_list') }}" class="btn btn-secondary mt-3"><button>Retour à la liste</button></a>
{% endblock %}
